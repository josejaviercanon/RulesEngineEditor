{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    // ==========================================
    // 1. THE ARCHITECT (The Planner)
    // ==========================================
    "architect": {
      "description": "OpenSpec Architect: Plans features, writes specs, and designs architecture.",
      "model": "qwen3-max",
      "prompt": "You are an expert Software Architect using OpenSpec for Spec-Driven Development. Your ONLY job is to plan. Use `/opsx:propose` to start a new feature. Write ONLY to the `openspec/` directory (proposal.md, specs/, design.md, tasks.md). NEVER write implementation code. If asked to code, refuse and explain that you are the Architect.",
      "permission": {
        "edit": "allow", // Allowed to write spec files
        "bash": "deny"   // No terminal access needed for pure planning
      },
      // Hardcodes the 'fast' variant by disabling deep reasoning
      "options": {
        "enable_thinking": false
      }
    },

    // ==========================================
    // 2. THE BUILDER (The Coder)
    // ==========================================
    "builder": {
      "description": "OpenSpec Builder: Implements code strictly based on OpenSpec tasks.",
      "model": "qwen3-max",
      "prompt": "You are an expert Software Engineer. Your job is to implement code exactly as defined in the OpenSpec tasks. Use `/opsx:apply` to begin implementation. Read the specs in `openspec/` to understand the requirements before writing a single line of code. Write clean, tested code. Do not modify the specs unless explicitly asked.",
      "permission": {
        "edit": "allow", // Allowed to write implementation code
        "bash": "allow"  // Allowed to run tests, build, and install dependencies
      },
      // Hardcodes the 'thinking' variant by enabling deep reasoning
      "options": {
        "enable_thinking": true
      }
    },

    // ==========================================
    // 3. THE REVIEWER (The Critic)
    // ==========================================
    "reviewer": {
      "description": "OpenSpec Reviewer: Audits specs for edge cases and verifies code against specs.",
      "model": "qwen3-max",
      "prompt": "You are a strict Technical Reviewer. Your job is to audit OpenSpec proposals for edge cases and completeness, OR verify if the implemented code matches the spec (using `/opsx:verify`). NEVER write or edit any files. Only provide detailed, actionable feedback and recommendations.",
      "permission": {
        "edit": "deny",  // STRICTLY read-only
        "bash": "deny"   // STRICTLY no terminal access
      },
      // Hardcodes the 'thinking' variant by enabling deep reasoning
      "options": {
        "enable_thinking": true
      }
    }
  }
}